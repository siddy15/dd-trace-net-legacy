import { InstrumentationBase, InstrumentationConfig } from '@opentelemetry/instrumentation';
import { CoralogixRumLabels, CustomMeasurementData } from '../../types';
export declare class CoralogixCustomMeasurementInstrumentation extends InstrumentationBase {
    constructor(config: InstrumentationConfig);
    protected init(): void;
    disable(): void;
    enable(): void;
    createCustomMeasurement(name: string, value: CustomMeasurementData, labels?: CoralogixRumLabels): void;
}
