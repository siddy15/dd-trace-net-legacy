import { CoralogixRumLabels } from '../../types';
import { MeasurementResult } from './custom-measurements.types';
export declare class TimeMeasurementTracker {
    private measurements;
    startMeasurement(key: string, labels?: CoralogixRumLabels): void;
    getMeasurement(key: string): MeasurementResult | undefined;
    private checkAndResetIfSessionExpired;
    private logDebug;
}
