export declare class HeadersManager {
    filterHeaders(headersInit: HeadersInit | string | undefined, allowedKeys?: string[]): Map<string, string>;
    private normalizeHeaders;
    private normalizeHeadersFromString;
}
